# Esto es equivalente a: from customwidgets.widgets import ImageButton
#:import ImageButton customwidgets.widgets.ImageButton
#:import SettingsScreen customwidgets.widgets.SettingsScreen

ScreenManager:
    id: screen_manager
    MenuScreen:
        id: menu_screen
        name: 'menu'
        manager: 'screen_manager'
    SettingsScreen:
        id: settings_screen
        name: 'settings'
        manager: 'screen_manager'

<MenuScreen>:
    GridLayout:
        cols: 4
        ImageButton:
            custom: 'rocket'
            source: './src/img/rocket_128.png'
            on_release: root.my_callback('rocket Button pressed')
        ImageButton:
            custom: 'pin'
            source: './src/img/pin_128.png'
            on_release: root.my_callback('pin Button pressed')
        ImageButton:
            custom: 'postbox'
            source: './src/img/postbox_128.png'
            on_release: root.my_callback('postbox Button pressed')
        ImageButton:
            custom: 'setting'
            source: './src/img/setting_128.png'
            on_release:
                root.my_callback('settings Button pressed')
                app.root.current = 'settings'
                app.root.transition.direction = 'left'

<SettingsScreen>: